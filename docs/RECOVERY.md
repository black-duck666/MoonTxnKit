# 恢复语义

每个有写入的成功事务生成一条 `WalRecord`，包含事务 ID、快照版本、提交版本、
整批写意图和校验值。

恢复分两阶段：

1. 预检全部输入记录的校验值、提交版本连续性和重复版本一致性；
2. 只有预检通过后才按版本应用整批写。

已存在且校验值相同的提交版本会被幂等跳过；版本相同但内容不同、版本断层或
校验失败都会停止恢复。

逻辑 WAL 只定义记录内容和重放语义。写入文件、刷盘顺序、进程崩溃原子性和
远端复制由外部适配器负责。
