# 公开开发追踪

## 已完成

- 项目骨架和事务领域模型；
- 快照读、读己之写和历史读取；
- Snapshot 写写冲突检测；
- Serializable 读集验证；
- 保存点和覆盖写回滚修复；
- 逻辑 WAL、校验、恢复和幂等重放；
- 低水位压缩、统计和一致性检查；
- 17 项测试和 CLI 场景。

## 后续工单

- `feature: 增加只读事务快照导出`
- `feature: 提供 Bytes 值适配包`
- `feature: 增加 WAL 二进制编解码`
- `test: 增加随机事务模型对照`
- `benchmark: 建立热点冲突与重放基准`

## 维护约定

- 每项语义修改必须同时提供冲突或恢复测试；
- 不把未实现的持久化保证写入 README；
- 报名前核对 GitHub、GitLink 的分支 SHA 和 CI；
- Issue、PR、变更记录和阶段成果持续公开。
