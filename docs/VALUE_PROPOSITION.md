# 项目价值

MoonTxnKit 将事务语义从磁盘、线程和业务对象中分离，适用于：

- 工作流任务领取和幂等消费；
- 库存、配额和槽位预占；
- 规则引擎的同快照事实判断；
- 离线工具与浏览器状态；
- 可重复的并发冲突测试。

与普通条件更新工具相比，项目同时提供历史快照、点读和谓词冲突、保存点、
WAL 恢复与版本压缩。与 SQL 数据库相比，它是可嵌入的纯状态事务内核，不承担
查询语言、页面管理和分布式复制。

`AtomicPlan` 适合已知键条件；`scan_prefix` 适合集合条件。二者共同覆盖
“单条状态必须仍成立”和“某类对象集合必须未变化”两类业务不变量。
