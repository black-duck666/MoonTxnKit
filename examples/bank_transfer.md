# 银行转账场景

`moon run cmd/main` 建立两个账户，随后执行以下流程：

1. 在版本 1 打开长读事务；
2. Serializable 事务把账户 A 从 100 调整为 70、账户 B 调整为 130；
3. 长读事务仍看到版本 1 的 A=100；
4. 两个并发事务写账户 A，后提交者收到 WriteWrite 冲突；
5. 用三条 WAL 恢复新引擎；
6. 活跃长快照结束后压缩旧版本。

输出全部为稳定文本或 JSON，可直接用于评审和差异测试。
